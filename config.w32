// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cassandra", "for cassandra support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cassandra", "enable cassandra support", "no");

if (PHP_CASSANDRA != "no") {
	EXTENSION("cassandra", "cassandra.c");
}

